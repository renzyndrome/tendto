{
  "name": "tendto-auth",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "bun --env-file=../../.env --watch src/index.ts",
    "start": "bun src/index.ts",
    "typecheck": "tsc --noEmit",
    "migrate": "bun --env-file=../../.env better-auth migrate"
  },
  "dependencies": {
    "@better-auth/cli": "latest",
    "better-auth": "latest",
    "hono": "latest",
    "pg": "latest"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/pg": "latest",
    "typescript": "~5.6.0"
  },
  "trustedDependencies": []
}
