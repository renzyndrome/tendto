{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "What the TendTo webview is allowed to ask Rust to do.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "notification:default",
    "powersync:default"
  ]
}
